name: Auto M3U Generator

on:
  push:
  workflow_dispatch:
  schedule:
    - cron: "0 */6 * * *"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Kodu al
      uses: actions/checkout@v3
      with:
        fetch-depth: 0   # Pull yapabilmek için şart!

    - name: 🐍 Python Kur
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: 📦 Gereksinimleri yükle
      run: |
        pip install requests

    - name: 🚀 M3U tarayıcıyı çalıştır
      run: |
        python auto_m3u_scanner.py

    - name: 🔧 Git pull --rebase
      run: |
        git config --global user.name "github-actions"
        git config --global user.email "github-actions@github.com"
        git pull --rebase origin main || true

    - name: 📤 Güncellenen playlist.m3u'yu commit et & pushla
      run: |
        git add playlist.m3u
        git commit -m "Auto update playlist.m3u" || echo "No changes"
        git push origin main
