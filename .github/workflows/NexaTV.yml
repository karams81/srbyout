name: NexaTV M3U Generator

on:
  schedule:
    - cron: "0 3 * * *"    # Her gün sabah 03:00 UTC
  workflow_dispatch:        # Manuel tetikleme

jobs:
  generate:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Repo'yu indir
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # Push işlemleri için ZORUNLU

      - name: Python kur
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Gerekli kütüphaneleri kur
        run: |
          python -m pip install --upgrade pip

      - name: NexaTV.py çalıştır
        run: python NexaTV.py

      - name: M3U dosyasını commit & force push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add NexaTV.m3u
          git commit -m "🔁 NexaTV.m3u otomatik güncelleme ($(date +'%Y-%m-%d %H:%M'))" || echo "Değişiklik yok"
          git push --force origin main
